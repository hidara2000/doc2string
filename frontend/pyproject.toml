[project]
name = "doc2string"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "reflex>=0.7.4",
]
[tool.setuptools]
package-dir = {"" = "reflex_ui"}

